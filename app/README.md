# Carpeta `app`

Contiene rutas de Expo Router.

- `_layout.tsx`: registra proveedor global de sesión y stack raíz.
- `index.tsx`: muestra login o redirige a tabs si existe sesión.
- `(tabs)/`: agrupa la navegación inferior principal.

Esta carpeta conecta navegación nativa con las pantallas de `src/screens`.
